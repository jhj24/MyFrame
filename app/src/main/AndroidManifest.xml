<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zgdj.djframe">

    <!-- 推送相关 start -->
    <!-- Required -->
    <permission
        android:name="com.zgdj.djframe.permission.JPUSH_MESSAGE"
        android:protectionLevel="signature" />
    <!-- Required  一些系统要求的权限，如访问网络等 -->
    <uses-permission android:name="com.zgdj.djframe.permission.JPUSH_MESSAGE" />
    <uses-permission android:name="android.permission.RECEIVE_USER_PRESENT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 推送相关 end -->


    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 震动权限 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 读取设置 -->
    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS" />
    <!-- 这个权限用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 这个权限用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 相机权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 指纹识别 -->
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- Unity 3D 相关设置 -->

    <uses-feature android:glEsVersion="0x00020000" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen.multitouch"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen.multitouch.distinct"
        android:required="false" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <!-- Unity 3D 相关设置 -->
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.zgdj.djframe.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_path" />
        </provider>

        <!-- 百度地图 key -->
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="5cIqhd90j9Qir00o6M5PrSOWKUttSIxn" />

        <!-- 百度定位 -->
        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:process=":remote" />

        <!-- 欢迎页 -->
        <activity
            android:name=".activity.home.WelcomeActivity"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Main 页面 -->
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.message.MessageDetailsActivity" />
        <activity android:name=".activity.other.Test1Activity" />
        <activity android:name=".activity.other.RecyclerViewActivity" />
        <activity android:name=".activity.other.MapActivity" />
        <activity android:name=".activity.personal.SettingActivity" />
        <!-- 扫描 -->
        <activity android:name=".activity.other.ScanActivity" />
        <!-- U3D -->
        <activity
            android:name=".activity.other.Unity3DActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:hardwareAccelerated="true" />
        <activity android:name=".activity.work.SupervisionLogActivity" />
        <activity android:name=".activity.personal.LoginActivity" />
        <!-- webView -->
        <activity
            android:name=".activity.other.WebViewActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".activity.work.QualityControlActivity" />
        <activity android:name=".activity.other.FingerprintActivity" />
        <activity android:name=".activity.other.PaletteActivity" />
        <activity android:name=".activity.work.QualityControlWriteActivity" />
        <activity android:name=".activity.other.PhotoViewActivity" />
        <activity android:name=".activity.work.ModelRealActivity" />
        <activity android:name=".activity.other.ImageDragZoomActivity" />
        <activity android:name=".activity.work.QualityEvaluationActivity" />
        <activity android:name=".activity.work.QualityEvaluationContentActivity" />
        <activity android:name=".activity.work.QualityEvaluationContentDetailActivity" />
        <activity android:name=".activity.other.WebH5Activity" />
        <activity android:name=".activity.work.DocumentTreeActivity" />
        <activity android:name=".activity.work.DocumentFileActivity" />
        <activity android:name=".activity.work.DocumentFileChildActivity" />
        <activity android:name=".activity.work.DocumentDownloadHistoryListActivity" />
        <activity android:name=".activity.work.DocumentFileEditActivity" />
        <activity android:name=".activity.work.DocumentFileChildEditActivity" />
        <activity android:name=".activity.work.DocumentShareActivity" />
        <activity android:name=".activity.work.DocumentShareSelectorActivity" />
        <activity android:name=".activity.work.StandardTreeActivity" />
        <activity android:name=".activity.work.StandardFileActivity" />
        <activity android:name=".activity.work.StandardFileEditActivity" />
        <activity android:name=".activity.other.PDFViewerActivity" />

        <!-- 推送 相关  start -->
        <!-- Required SDK核心功能 -->
        <activity
            android:name="cn.jpush.android.ui.PushActivity"
            android:configChanges="orientation|keyboardHidden"
            android:exported="false"
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="cn.jpush.android.ui.PushActivity" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="com.zgdj.djframe" />
            </intent-filter>
        </activity>

        <!-- Rich push 核心功能 since 2.0.6 -->
        <activity
            android:name="cn.jpush.android.ui.PopWinActivity"
            android:exported="false" />

        <!-- Required SDK 核心功能 -->
        <!-- 可配置android:process参数将PushService放在其他进程中 -->
        <service
            android:name="cn.jpush.android.service.PushService"
            android:exported="false"
            android:process=":mult">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.REGISTER" />
                <action android:name="cn.jpush.android.intent.REPORT" />
                <action android:name="cn.jpush.android.intent.PushService" />
                <action android:name="cn.jpush.android.intent.PUSH_TIME" />
            </intent-filter>
        </service>
        <!-- since 3.0.9 Required SDK 核心功能 -->
        <provider
            android:name="cn.jpush.android.service.DataProvider"
            android:authorities="com.zgdj.djframe.DataProvider"
            android:exported="false" />
        <!-- since 1.8.0 option 可选项。用于同一设备中不同应用的JPush服务相互拉起的功能。 -->
        <!-- 若不启用该功能可删除该组件，将不拉起其他应用也不能被其他应用拉起 -->
        <service
            android:name="cn.jpush.android.service.DaemonService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.DaemonService" />

                <category android:name="com.zgdj.djframe" />
            </intent-filter>
        </service>
        <!-- since 3.1.0 Required SDK 核心功能 -->
        <provider
            android:name="cn.jpush.android.service.DownloadProvider"
            android:authorities="com.zgdj.djframe.DownloadProvider"
            android:exported="true" />

        <!-- Required SDK核心功能 -->
        <receiver
            android:name="cn.jpush.android.service.PushReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="cn.jpush.android.intent.NOTIFICATION_RECEIVED_PROXY" /> <!-- Required  显示通知栏 -->
                <category android:name="com.zgdj.djframe" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
            <!-- Optional -->
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />

                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- Required SDK核心功能 -->
        <receiver
            android:name="cn.jpush.android.service.AlarmReceiver"
            android:exported="false" />

        <!-- User defined.  For test only  用户自定义的广播接收器 -->
        <receiver
            android:name=".jpush.MyReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.REGISTRATION" /> <!-- Required  用户注册SDK的intent -->
                <action android:name="cn.jpush.android.intent.MESSAGE_RECEIVED" /> <!-- Required  用户接收SDK消息的intent -->
                <action android:name="cn.jpush.android.intent.NOTIFICATION_RECEIVED" /> <!-- Required  用户接收SDK通知栏信息的intent -->
                <action android:name="cn.jpush.android.intent.NOTIFICATION_OPENED" /> <!-- Required  用户打开自定义通知栏的intent -->
                <action android:name="cn.jpush.android.intent.CONNECTION" /> <!-- 接收网络变化 连接/断开 since 1.6.3 -->
                <category android:name="com.zgdj.djframe" />
            </intent-filter>
        </receiver>

        <!-- User defined.  For test only  用户自定义接收消息器,3.0.7开始支持,目前新tag/alias接口设置结果会在该广播接收器对应的方法中回调 -->
        <receiver android:name=".jpush.MyJPushMessageReceiver">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.RECEIVE_MESSAGE" />

                <category android:name="com.zgdj.djframe" />
            </intent-filter>
        </receiver>

        <!-- Required  . Enable it you can get statistics data with channel -->
        <meta-data
            android:name="JPUSH_CHANNEL"
            android:value="developer-default" />
        <meta-data
            android:name="JPUSH_APPKEY"
            android:value="710465ebb901366c0a1cfc40" /> <!-- </>值来自开发者平台取得的AppKey -->


        <!-- 推送 相关  end -->

        <activity android:name=".activity.work.ModelAllActivity" />
        <activity android:name=".activity.message.MessageHistoryActivity" />
        <activity android:name=".activity.message.MessageApprovalActivity" />
        <activity android:name=".activity.message.MessageApproval2Activity" />
        <activity android:name=".activity.message.ChoiceOfExecutorActivity" />
        <activity android:name=".activity.message.MessageRefuseApprovalActivity" />
        <activity android:name=".activity.work.progress.ProgressRealTimeTreeActivity" />
        <activity android:name=".activity.work.progress.ProgressRealTimeActivity" />
        <activity android:name=".activity.work.progress.ProgressRealTimeAddActivity" />
        <activity android:name=".activity.work.ProgressRealTimeActivity" />
        <activity
            android:name=".activity.work.ProgressRealTimeAddActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".activity.message.MessageRemindActivity" />
        <activity android:name=".activity.message.MessageWarmingActivity" />
        <activity android:name=".activity.message.MessageTaskListActivity"></activity>
    </application>

</manifest>